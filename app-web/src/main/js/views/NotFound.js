// @formatter:off
define([
], function(
) {
// @formatter:on
    var NotFound = function(dom, initOptions) {
        NotFound.superclass.constructor.apply(this, arguments);
    };
    violet.oo.extend(NotFound, violet.ui.ViewBase);

    return NotFound;
});
